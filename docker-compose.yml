services:

  db:
    image: postgres:17
    restart: always
    shm_size: 128mb
    ports:
    - "127.0.0.1:5432:5432"
    env_file:
      - .env
    volumes:
      - ./db/dump:/dump
      - ./db/data:/var/lib/postgresql/data
